# Changes / Open Issues — 2026-09-01

Review of the `refactor` branch of USACE-NSI/Geospatial (HEAD `9ec2296`).
Priorities: **P0** = correctness / data loss, **P1** = robustness / latent crashes,
**P2** = minor / consistency, **P3** = housekeeping.

| ID | Priority | File / Component | Issue | Impact | Recommended fix |
|----|----------|------------------|-------|--------|-----------------|
| P0-1 | P0 | `Spatial/RTreeNode.cs` | `getMBRoverlap` gate returns 0 when the query box is fully *contained* in a node box; insert and query paths prune the correct subtree | Features silently go missing once the tree grows; proving test is `[Fact(Skip=...)]` | Port the closed-interval overlap test (already in `BoundingBox.Overlaps`, "fix(#15)") into `RTreeNode`; re-enable `BulkInsert_AllFeaturesFindableByPoint` |
| P0-2 | P0 | `Spatial/SpatialJoins.cs` | Both joins build an R-tree and discard it (`_ = pointTree ?? BuildTree(points)`); `pointTree`/`polyTree` params are dead | O(n·m) full scan; public API falsely advertises tree-based candidate selection | Wire the tree into candidate selection (after P0-1) or remove the params and `BuildTree` |
| P0-4 | P0 | `Io/SpatialWriter.cs` | `SetOgrField` casts `long` → `int`; `FieldType.Long` maps to `OFTString`; bools written as `"1"`/`"0"` but `Coerce` uses `bool.TryParse` | 64-bit IDs > 2³¹ corrupted; Long round-trips as text; bools coerce to null on read-back | `SetField(name, l)` + `OFTInteger64`; pick one bool representation and parse it |
| P0-5 | P0 | `Io/SpatialReader.cs` | `CloseRing()` called on linestrings and points | Appends first vertex to open lines (inflated perimeter, polluted distances); duplicates point vertices | Call `CloseRing` only for polygon rings |
| P0-6 | P0 | `Spatial/SpatialJoins.cs` | `JoinType.Average` guards on the unfiltered count; filtered sequence can be empty | `InvalidOperationException` on valid input (all values non-numeric) | Filter first, guard on filtered count |
| P0-7 | P0 | `Spatial/SpatialJoins.cs` | `DistanceFeatureToFeature` reads `Vertices[0]` on empty parts; point coords taken from possibly-`Empty` `Mbr` | `IndexOutOfRangeException`; garbage distances when `ComputeMbr()` wasn't called | Guard empty parts; validate `Mbr != BoundingBox.Empty` |
| P0-8 | P0 | `Io/Csv/CsvHelper.cs` | Writer quotes embedded newlines (multi-line fields) but `ReadAll`/`ParseLine` reset state per line | Write→read round-trip corrupts values containing `\n` | Accumulate lines while a quote is open, or document as write-only |
| P0-9 | P0 | `Attributes/AttributeTable.cs` | `RenameColumn` moves only the schema key; per-feature `Attributes` dicts keep the old key | `CoerceRow` silently drops the renamed column | Propagate renames to rows or document as schema-only |
| P1-1 | P1 | `Spatial/RTreeNode.cs` | Split-position loop empty when `maxChildren < 2*minChildren − 1` → `Options.First()` throws | Insert crashes with near-default configs (e.g. min 4 / max 6) | Validate `max >= 2*min − 1` in `RTreeManager` ctor |
| P1-2 | P1 | `Spatial/RTreeNode.cs` | `getChildrenContainingInd` dereferences `node._featureIndex` unconditionally | NRE if an internal node ends up under an end node | Null-guard the index |
| P1-3 | P1 | `Spatial/RTreeManager.cs` | `public _root` reassigned on root split | Callers caching the root hold a detached tree | Encapsulate root access |
| P1-4 | P1 | `Geometry/BoundingBox.cs` | `Empty` sentinel normalizes to a whole-double-range box; `FromVertices` +Inf check is dead; `ContainsPoint(Empty)` true for all points | Subtle misbehavior for anyone not doing exact equality | Use an `IsEmpty` flag (or nullable box) |
| P1-5 | P1 | `Spatial/RTreeManager.cs` | `addFeature(featInd, Xmax, Xmin, Ymax, Ymin)` — reversed order vs every other API in the repo | Easy silent swap of min/max | Accept a `BoundingBox` |
| P1-6 | P1 | `Geometry/Feature.cs` | `Parts` is public; `Mbr` only maintained by `AddPart` | Direct `Parts.Add` leaves stale `Mbr` (tests rely on manual `ComputeMbr()`) | Expose `IReadOnlyList<Part>` or recompute lazily |
| P1-7 | P1 | `Geometry/FeatureCollection.cs` | `RemoveFeature` renumbers `Id`s | Desyncs previously built trees; removed feature keeps old `Id` | Rebuild on demand or document |
| P1-8 | P1 | `Spatial/SpatialJoins.cs` | `BuildTree` keys on `f.Id`, not collection position | `findByInd` useless for features not added via `AddFeature` (all `Id == 0`) | Use loop index or throw when `Id` unset |
| P1-9 | P1 | `Attributes/AttributeColumn.cs` | `Coerce` uses current-culture `ToString`/`TryParse` | Wrong values under non-invariant cultures (e.g. `de-DE`) | Invariant-culture parsing (+ `NumberStyles`/`DateTimeStyles`) |
| P1-10 | P1 | `Reprojection/Reprojector.cs` | `CrsToken` silently prefers EPSG over WKT; P/Invoke targets *private* OGR symbols (`OGRNewCoordinateTransformation`, …) | Custom WKT definitions ignored; private-API dependency | Prefer WKT (or throw on conflict); switch to stable `GDALCreateCoordinateTransformation` API (GDAL ≥ 3.7; repo pins 3.11.3) |
| P1-11 | P1 | `Io/SpatialReader.cs`, `Io/SpatialWriter.cs` | OGR ownership: `using` on `GetFieldDefnRef` may free shared layer state; `using` on feature passed to `CreateFeature` may double-free | Native memory corruption / leaks | Verify ownership semantics against the 3.11.3 binding; add focused tests |
| P1-13 | P1 | `Io/SpatialReader.cs` | `MapGeomType` defaults unknown types to `Point`; multipolygon rings flattened into one part list | MultiLine/MultiPoint/collections mislabeled with empty geometry; exterior↔hole association lost | Handle types explicitly; preserve ring structure |
| P2-1 | P2 | `Geometry/Part.cs` | `IsHole = !Direction` inverted for shapefile convention (outer rings CCW) | Latent — fields are dead except inside `CloseRing` | Fix polarity or delete the fields |
| P2-2 | P2 | `Spatial/SpatialJoins.cs` | Tie-break epsilon `1e-9` in absolute CRS units | Inconsistent nearest-neighbor selection across CRSes | Relative tolerance or parameter |
| P2-3 | P2 | `Spatial/SpatialJoins.cs` | Sum/Average/Count store numbers into backfilled source-typed columns (possibly Text) | Type mismatch in joined output | Widen backfilled column type |
| P2-4 | P2 | `Feature` / `Part` / `FeatureCollection` | `Wkt` properties actually hold SRS WKT, not geometry WKT | Misleading names | Rename to `SrsWkt` |
| P2-5 | P2 | `Spatial/RTreeManager.cs`, `RTreeNode.cs` | Public underscore fields, camelCase methods, `maxChidrens`/`minChidrens` typos, unused `System.Xml` usings, block-scoped namespaces | Conflicts with README conventions; nullable warnings swallowed (`TreatWarningsAsErrors=false`) | Style cleanup; consider re-enabling warnings-as-errors |
| P2-6 | P2 | `Spatial/SpatialJoins.cs` | `interiorOnly` unused; `ContainsIndex` helper for a `==` comparison | Dead API surface | Remove / rename |
| P2-7 | P2 | `Spatial/RTreeNode.cs` | Dead code: `addFeatureChild`, `cumulativeOverlap`/`siblingOverlap`, `RecomputeMBR`, `getCandidateFeatNodesByMBR` | Maintenance noise | Remove |
| P2-8 | P2 | `Attributes/AttributeTable.cs` | Column order relies on `Dictionary` insertion order | DBF field order depends on an implementation detail | Back with `List<AttributeColumn>` + name index |
| P2-9 | P2 | `Io/SpatialReader.cs` | `Ogr.RegisterAll()` per call; only layer 0 read; unset text → `""` vs unset date → `null` | Multi-layer GPKG ignored; inconsistent null semantics | Generalize or document |
| P2-10 | P2 | `global.json` | Pins SDK `9.0.100` while README says ".NET 8 is enough" | .NET 8-only machines can't build; CI 8.0.x install is decorative | Align pin with docs |
| P3-1 | P3 | `README.md` | Four inaccuracies: `Nsi.Geospatial.Core` name, `TreatWarningsAsErrors` claim, CSV behind interfaces claim, .NET 8 claim | Misleads contributors | Fix docs |
| P3-2 | P3 | `tests/Nsi.Geospatial.Tests/Nsi.Geospatial.Tests.csproj` | Missing `<IsPackable>false</IsPackable>` (Io.Tests has it) | Whole-solution `dotnet pack` would emit test packages | Add the property |
| P3-3 | P3 | `.github/workflows/ci.yml` | `--filter Category!=Gdal` is a no-op (no test declares the trait) | Gdal-category tests would silently never run | Add a native-GDAL CI job when such tests land |
| P3-4 | P3 | `*.csproj` | `PackageReference Include="gdal"` (lowercase) | Non-canonical package id | Use `GDAL` |
| P3-5 | P3 | `.github/workflows/release.yml` | Reusable workflow pinned to mutable `@main` of another org | Supply-chain / availability risk | Pin a tag or commit |